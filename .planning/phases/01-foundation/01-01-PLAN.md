---
phase: 01-foundation
plan: 01
type: execute
depends_on: []
files_modified: [pom.xml, src/main/java/com/hyvote/votelistener/.gitkeep]
---

<objective>
Set up Maven project structure with pom.xml and standard directory layout.

Purpose: Establish the build system and directory structure that all subsequent development depends on.
Output: Compilable Maven project with correct dependencies for Hytale plugin development.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
.planning/phases/01-foundation/01-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Tech stack (from PROJECT.md):**
- Java 25
- Maven build
- Must match hytale-votifier conventions

**Key conventions to follow:**
- Group ID: `com.hyvote`
- Artifact ID: `hytale-votelistener`
- Base package: `com.hyvote.votelistener`
- HytaleServer.jar contains API dependencies (scope: provided)
- GSON for JSON parsing (scope: compile)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pom.xml with Maven configuration</name>
  <files>pom.xml</files>
  <action>
Create pom.xml at project root with:
- groupId: com.hyvote
- artifactId: hytale-votelistener
- version: 1.0.0-SNAPSHOT
- packaging: jar
- Java 25 compiler settings
- Dependencies:
  - HytaleServer (scope: system, systemPath pointing to HytaleServer.jar) - provides CoreServer, Common, Event, Logger APIs
  - GSON 2.10.1 (scope: compile) for JSON config parsing
- Maven compiler plugin 3.11.0 with source/target 25
- Maven jar plugin 3.3.0
- Maven shade plugin 3.5.1 to bundle GSON into final JAR

Use system scope for HytaleServer.jar since it's a local file, NOT a Maven repository artifact.
  </action>
  <verify>mvn validate succeeds without errors</verify>
  <done>pom.xml exists, Maven validates successfully, dependencies resolve</done>
</task>

<task type="auto">
  <name>Task 2: Create Maven directory structure</name>
  <files>src/main/java/com/hyvote/votelistener/.gitkeep, src/main/resources/.gitkeep</files>
  <action>
Create standard Maven directory structure:
- src/main/java/com/hyvote/votelistener/ (main source package)
- src/main/resources/ (for config files)

Add .gitkeep files to preserve empty directories in git.
  </action>
  <verify>ls -la src/main/java/com/hyvote/votelistener/ shows directory exists</verify>
  <done>Directory structure exists matching Maven conventions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mvn validate` succeeds
- [ ] `mvn compile` succeeds (no sources yet, but config valid)
- [ ] Directory structure matches Maven standard layout
</verification>

<success_criteria>

- pom.xml valid and complete
- Maven validates successfully
- Directory structure ready for source code
- Project compiles (empty)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
